# แผนการสร้างระบบรายงานผลการดำเนินงานตาม MOU

> เอกสารนี้คือแผนหลักของโครงการ **ทุกครั้งที่มีการเปลี่ยนแปลงหรือความคืบหน้า ให้อัปเดตไฟล์นี้**

---

## Overview

**ระบบรายงานผลการดำเนินงานตามบันทึกข้อตกลง (MOU) ของการยางแห่งประเทศไทย**

เว็บแอปพลิเคชันสำหรับรายงานและติดตามผลการดำเนินงานตาม MOU ประจำปีบัญชี
ของส่วนงานและหน่วยงานที่ไม่สังกัดส่วนงาน รวม 30 หน่วย

| หัวข้อ | รายละเอียด |
|---|---|
| **องค์กร** | การยางแห่งประเทศไทย (กยท. / RAOT) |
| **ปีบัญชีปัจจุบัน** | 2569 (1 ต.ค. 2568 – 30 ก.ย. 2569) |
| **จำนวนหน่วยงาน** | 30 หน่วย (ส่วนงาน 20 + หน่วยงานที่ไม่สังกัดส่วนงาน 10) |
| **จำนวนตัวชี้วัด** | 301 รายการ (ดึงจาก MOU ฉบับลงนามครบทุกฉบับแล้ว) |
| **เกณฑ์คะแนน** | 5 ระดับต่อตัวชี้วัด รวม 1,505 รายการ |
| **รอบรายงาน** | รายไตรมาส (4 ครั้งต่อปีบัญชี) |
| **เทคโนโลยี** | Next.js + TypeScript + Tailwind + Prisma + PostgreSQL (Neon) |
| **การนำขึ้นระบบ** | **Vercel** (ไม่ต้องติดตั้งอะไรในเครื่อง ไม่ต้องมีเซิร์ฟเวอร์เอง) |

### ฟังก์ชันที่ต้องมี (จากโจทย์ตั้งต้น)

| # | ฟังก์ชัน | Phase |
|---|---|---|
| 1 | แสดงตัวชี้วัดของส่วนงาน | 4 |
| 2 | เพิ่มตัวชี้วัด | 4 |
| 3 | แก้ไขตัวชี้วัด | 4 |
| 4 | แสดง % ความก้าวหน้า (จากไฟล์ผลการดำเนินงานรายไตรมาส) | 7 |
| 5 | รายงานผลตามตัวชี้วัดและเกณฑ์คะแนน 1–5 | 7 |
| 6 | รายงานแผนการดำเนินงานของตัวชี้วัด | 6 |
| 7 | Dashboard สรุปจำนวนตัวชี้วัดของส่วนงาน | 9 |
| 8 | เพิ่มปีบัญชีได้ | 5 |
| 9 | ตั้งเวลาปิดระบบไม่ให้แนบเอกสารเพิ่ม | 8 |
| 10 | รองรับการใช้งานบนมือถือ | 10 |

### วงจรการใช้งานใน 1 ปีบัญชี

```
ส่วนกลาง (ADMIN)         ส่วนงาน (30 หน่วย)          ผู้บริหาร
────────────────         ──────────────────          ─────────
เปิดปีบัญชี
  ↓
สร้างตัวชี้วัดให้ทุกหน่วย  →  บันทึกแผนดำเนินงาน
  ↓                            ↓
เปิดรอบไตรมาส 1         →  กรอกผล + แนบไฟล์   →  ดู Dashboard
  ↓                            ↓                        ↓
ถึงเวลาปิดระบบ           ✗ แนบไฟล์ไม่ได้แล้ว          ดูรายงานคะแนน
  ↓
เปิดรอบไตรมาส 2 ... (วนซ้ำจนครบ 4 ไตรมาส)
  ↓
สรุปคะแนนรวมทั้งปี
```

---

## สถานะปัจจุบัน

| Phase | งาน | สถานะ |
|---|---|---|
| 0 | เตรียมข้อมูล (MOU 30 ฉบับ, รายชื่อหน่วยงาน, ตัวชี้วัด) | ✅ เสร็จ |
| 1 | ตั้งโปรเจกต์ Next.js + Tailwind + Prisma | ✅ เสร็จ |
| 2 | โครงฐานข้อมูล + ข้อมูลตั้งต้น | ✅ **เสร็จ ข้อมูลอยู่บนฐานข้อมูลจริงแล้ว** |
| 3 | ระบบ Login + สิทธิ์ตาม role | ✅ **เสร็จ ทดสอบ login จริงผ่านแล้ว** |
| 4 | จัดการตัวชี้วัด (ข้อ 1, 2, 3) | ✅ **เสร็จ ทดสอบการเจาะสิทธิ์ผ่านแล้ว** |
| 5 | จัดการผู้ใช้ + ปีบัญชี (ข้อ 8) | ✅ **เสร็จ ทดสอบการคัดลอกปี 2570 ผ่านแล้ว** |
| 6 | แผนการดำเนินงาน (ข้อ 6) | ✅ **เสร็จ ทดสอบผ่านเบราว์เซอร์จริงแล้ว** |
| 7 | รายงานผล + คำนวณ % และคะแนน (ข้อ 4, 5) | ✅ **เสร็จ ทดสอบผ่านเบราว์เซอร์จริงแล้ว** |
| 7b | ไฟล์แนบหลักฐาน (Vercel Blob) | 🟡 **เขียนโค้ดเสร็จแล้ว รอ token จาก Vercel เพื่อทดสอบ** |
| 8 | ตั้งเวลาเปิด-ปิดระบบ (ข้อ 9) | ✅ **เสร็จ ทดสอบว่าปิดแล้วบันทึกไม่ได้จริง** |
| 9 | Dashboard + รายงาน (ข้อ 7) | ✅ **เสร็จ พร้อม export Excel** |
| 10 | ปรับมือถือ + ขัดเกลา (ข้อ 10) | ✅ **เสร็จ ตรวจครบ 16 หน้าผ่านหมด** |
| 11 | ทดสอบ + ขึ้นระบบจริงบน Vercel | ⬜ ยังไม่เริ่ม |

### ฐานข้อมูลจริง (เชื่อมต่อแล้ว)

| หัวข้อ | ค่า |
|---|---|
| ผู้ให้บริการ | Neon (PostgreSQL 18) |
| โปรเจกต์ | `mou` (`frosty-field-00121876`) |
| Region | `aws-ap-southeast-1` สิงคโปร์ — ใกล้ไทยที่สุด |
| Branch | `production` |
| ไฟล์ config | `neon.ts` · `.neon/` (ทั้งคู่ไม่ขึ้น git) |

**ข้อมูลที่อยู่ในฐานข้อมูลแล้ว** (ตรวจสอบยืนยันจากฐานข้อมูลจริง):

| รายการ | จำนวน | ตรวจสอบ |
|---|---|---|
| ส่วนงาน | 20 | ✅ |
| หน่วยงานที่ไม่สังกัดส่วนงาน | 10 | ✅ |
| **ปีบัญชี** | **2 (2569 กำลังใช้งาน · 2570 เตรียมไว้)** | ✅ |
| ตัวชี้วัด | 602 (ปีละ 301) | ✅ ครบทุกส่วนงานทั้งสองปี |
| เกณฑ์คะแนน | 3,010 | ✅ (301 × 5 ระดับ × 2 ปี) |
| ช่วงเวลาเปิด-ปิด | 8 (ปีละ 4 ไตรมาส) | ✅ |
| บัญชีผู้ใช้ | 1 (ADMIN) | ✅ |
| น้ำหนักรวมของทุกส่วนงาน = 100 | 30/30 ทั้งสองปี | ✅ |
| ตัวชี้วัดที่ค่าน้อยยิ่งดี | 29 ต่อปี | ✅ |

> **ปี 2570 คัดลอกมาจากปี 2569 ไว้แล้ว** แต่ยังไม่เปิดใช้งาน ผู้ใช้ทั่วไปยังเห็นแต่ข้อมูลปี 2569
> เมื่อถึงเวลาขึ้นปีบัญชีใหม่จริง ให้แก้ตัวเลขเป้าหมายให้ตรง MOU ปี 2570 แล้วค่อยกดสลับปีที่ `/admin/fiscal-years`

### หน้าจอที่ใช้งานได้แล้ว

| URL | หน้าที่ | ใครเข้าได้ |
|---|---|---|
| `/login` | เข้าสู่ระบบ | ทุกคน |
| `/dashboard` | สรุปจำนวนตัวชี้วัดแยกตามมิติ | ทุก role |
| `/indicators` | รายการตัวชี้วัด + ค้นหา + กรองตามส่วนงาน/มิติ + แบ่งหน้า | ทุก role (DEPT_USER เห็นเฉพาะของตัวเอง) |
| `/indicators/[id]` | รายละเอียด + เกณฑ์คะแนน 5 ระดับ | ทุก role (ตามสิทธิ์การมองเห็น) |
| `/indicators/new` | เพิ่มตัวชี้วัด | **ADMIN เท่านั้น** |
| `/indicators/[id]/edit` | แก้ไขตัวชี้วัด | **ADMIN เท่านั้น** |
| `/account` | ดูข้อมูลบัญชี + เปลี่ยนรหัสผ่าน | ทุกคน |
| `/admin` | หน้ารวมเมนูตั้งค่า + สรุปสถานะระบบ | **ADMIN เท่านั้น** |
| `/admin/users` | รายชื่อผู้ใช้ + เตือนส่วนงานที่ยังไม่มีผู้รับผิดชอบ | **ADMIN เท่านั้น** |
| `/admin/users/new` | เพิ่มบัญชีผู้ใช้ + ตั้งรหัสผ่านเริ่มต้น | **ADMIN เท่านั้น** |
| `/admin/users/[id]` | แก้ไขบัญชี (สิทธิ์/สังกัด/เปิด-ปิด) + ตั้งรหัสผ่านใหม่ให้ | **ADMIN เท่านั้น** |
| `/admin/fiscal-years` | เพิ่มปีบัญชี · สลับปีที่ใช้งาน · คัดลอกตัวชี้วัดข้ามปี | **ADMIN เท่านั้น** |
| `/admin/windows` | ตั้งเวลาเปิด-ปิดรายไตรมาส · ปิดฉุกเฉิน · ขยายเวลาเฉพาะส่วนงาน | **ADMIN เท่านั้น** |
| `/plans` | รายการตัวชี้วัดพร้อมจำนวนกิจกรรมรายไตรมาส + ค้นหา/กรอง | ทุก role (DEPT_USER เห็นเฉพาะของตัวเอง) |
| `/plans/[indicatorId]` | วางแผนกิจกรรมรายไตรมาส เพิ่ม/แก้/ลบ + แถบความคืบหน้า | ส่วนงานเจ้าของ + ADMIN แก้ได้ · EXECUTIVE ดูอย่างเดียว |
| `/reports` | ตารางคะแนนรายไตรมาสของทุกตัวชี้วัด + ค้นหา/กรอง | ทุก role (DEPT_USER เห็นเฉพาะของตัวเอง) |
| `/reports/[indicatorId]/[quarter]` | กรอกผลงานจริง · ดู % และคะแนนสดๆ · ปรับคะแนนด้วยมือ · ส่ง/ดึงกลับ | ส่วนงานเจ้าของ + ADMIN กรอกได้ · EXECUTIVE ดูอย่างเดียว |
| `/dashboard` | ตัวเลขสรุป · สรุปตามมิติ · เปรียบเทียบ 30 ส่วนงาน · กรองรายไตรมาส · ดาวน์โหลด Excel | ทุก role (DEPT_USER เห็นเฉพาะของตัวเอง) |

### ผลการทดสอบความปลอดภัย (Phase 4)

ทดสอบด้วยบัญชี `DEPT_USER` สังกัด ฝกค. โดยพยายามข้ามการซ่อนปุ่ม:

| การทดสอบ | ผล |
|---|---|
| เห็นตัวชี้วัดกี่รายการ | 8 รายการ (เฉพาะของ ฝกค.) ✅ |
| เปิดตัวชี้วัดของส่วนงานอื่นด้วย URL ตรง | 404 ✅ |
| เปิดหน้าเพิ่ม/แก้ไขตัวชี้วัด | เด้งกลับ `/indicators` ✅ |
| เปิดหน้าตั้งค่าระบบ | เด้งกลับ `/dashboard` ✅ |
| ใส่ `?dept=` ของส่วนงานอื่นใน URL | ยังเห็น 8 รายการเท่าเดิม ✅ |
| **ยิง Server Action สร้างตัวชี้วัดตรงๆ** | ปฏิเสธ + ไม่มีข้อมูลถูกสร้าง ✅ |

การทดสอบข้อสุดท้ายสำคัญที่สุด เพราะเป็นการข้ามหน้าเว็บไปเรียกโค้ดฝั่งเซิร์ฟเวอร์โดยตรง
ซึ่งการซ่อนปุ่มบนหน้าจอกันไม่ได้

### ผลการทดสอบ Phase 5

**1. การกันสิทธิ์ของหน้าใหม่** (ยิงตรงโดยไม่ login):

| URL | ผล |
|---|---|
| `/admin` · `/admin/users` · `/admin/users/new` | เด้งไป `/login` ✅ |
| `/admin/users/<id ที่มั่วขึ้นมา>` · `/admin/fiscal-years` | เด้งไป `/login` ✅ |

**2. การเจาะสิทธิ์ด้วยบัญชีส่วนงาน** (สร้างบัญชีทดสอบ DEPT_USER สังกัด กจร.1 แล้วลองข้ามด่าน):

| การทดสอบ | ผล |
|---|---|
| เปิดหน้า `/admin` ทั้ง 5 หน้าด้วย URL ตรง | เด้งกลับ `/dashboard` ทุกหน้า ✅ |
| **ยิง Server Action สร้างบัญชี ADMIN ให้ตัวเอง** | ปฏิเสธ + ไม่มีบัญชีถูกสร้าง ✅ |
| **ยิง Server Action ตั้งรหัสผ่านใหม่ให้บัญชี ADMIN** | ปฏิเสธ + รหัสเดิมของ ADMIN ยังใช้ได้ ✅ |

สองข้อหลังสำคัญที่สุด เพราะเป็นการข้ามหน้าเว็บไปเรียกโค้ดฝั่งเซิร์ฟเวอร์ตรงๆ
ถ้ากันไม่อยู่ ผู้ใช้ทั่วไปจะยกระดับตัวเองเป็นผู้ดูแลระบบหรือยึดบัญชีคนอื่นได้

**3. ด่านกันตั้งค่าผิดจนใช้ระบบไม่ได้** (ทดสอบด้วยบัญชี ADMIN):

| การทดสอบ | ผล |
|---|---|
| ADMIN ลดสิทธิ์ตัวเองเป็นผู้รับผิดชอบส่วนงาน | ปฏิเสธ "ลดสิทธิ์ของตัวเองไม่ได้" ✅ |
| ADMIN ปิดใช้งานบัญชีตัวเอง | ปฏิเสธ "ปิดใช้งานบัญชีของตัวเองไม่ได้" ✅ |
| แก้อีเมลไปชนกับบัญชีอื่น | ปฏิเสธ พร้อมบอกว่าอีเมลไหนซ้ำ ✅ |
| ชื่อภาษาไทยที่กรอก | เก็บและแสดงผลถูกต้อง ไม่เพี้ยน ✅ |
| อีเมลตัวพิมพ์ใหญ่ (`Test.Phase5@Example.COM`) | แปลงเป็นตัวพิมพ์เล็กอัตโนมัติ ✅ |
| รหัสผ่านที่เก็บลงฐานข้อมูล | เข้ารหัส bcrypt (`$2b$`) ไม่ใช่ข้อความจริง ✅ |

**4. การคัดลอกตัวชี้วัดขึ้นปีใหม่** (ทดสอบกับฐานข้อมูลจริงผ่านหน้าเว็บจริง):

| การทดสอบ | ผล |
|---|---|
| เพิ่มปีบัญชี 2570 แล้วคัดลอกตัวชี้วัดจากปี 2569 | ได้ 301 ตัวชี้วัด + 1,505 เกณฑ์คะแนน ✅ |
| ตัวชี้วัดที่ค่าน้อยยิ่งดีในปีใหม่ | 29 รายการ เท่าปีเก่าเป๊ะ ✅ |
| น้ำหนักรวมของแต่ละส่วนงานในปีใหม่ | = 100 ครบทั้ง 30 ส่วนงาน ✅ |
| ข้อมูลปี 2569 หลังคัดลอก | ยังครบเท่าเดิม ไม่ถูกแตะ ✅ |
| ตัวชี้วัดสองปีใช้ id เดียวกันหรือไม่ | ไม่ซ้ำเลย แยกกันสนิท ✅ |
| ผลการดำเนินงานถูกคัดลอกตามมาหรือไม่ | ไม่ถูกคัดลอก ปีใหม่เริ่มจากศูนย์ ✅ |
| กดคัดลอกซ้ำครั้งที่ 2 | คัดลอก 0 รายการ ไม่เกิดข้อมูลซ้ำ ✅ |
| เลือกปีต้นทางกับปลายทางเป็นปีเดียวกัน | ปฏิเสธ ✅ |
| สลับไปใช้ปี 2570 แล้วสลับกลับ 2569 | มีปีที่ใช้งานเหลือปีเดียวเสมอ · หน้าตัวชี้วัดเปลี่ยนตาม ✅ |
| ประวัติการใช้งาน (AuditLog) | บันทึกครบทุกการกระทำ ส่วนที่ถูกปฏิเสธไม่ถูกบันทึก ✅ |

**5. การแปลงเวลาไทย ↔ UTC** (จุดที่พลาดง่ายที่สุด ทดสอบด้วยสคริปต์และจากฐานข้อมูลจริง):

| ค่าที่กรอก | เก็บลงฐานข้อมูล (UTC) | แสดงบนหน้าจอ |
|---|---|---|
| 1 ต.ค. 2569 (ต้นปีบัญชี) | `2026-09-30T17:00:00Z` | 1 ต.ค. 2569 00:00 น. ✅ |
| 30 ก.ย. 2570 (ปลายปีบัญชี) | `2027-09-30T16:59:59Z` | 30 ก.ย. 2570 23:59 น. ✅ |

แปลงกลับเข้าช่องกรอกวันที่ได้ค่าเดิมเป๊ะ ไม่มีอาการเคลื่อน 7 ชั่วโมง

### ผลการทดสอบ Phase 6

ทดสอบด้วยเบราว์เซอร์จริง (กดปุ่มจริงเหมือนผู้ใช้) บนฐานข้อมูลจริง

| การทดสอบ | ผล |
|---|---|
| เพิ่มกิจกรรมในไตรมาส 1 จำนวน 2 รายการ | บันทึกครบ พร้อมผลผลิตที่คาดหวังและสถานะ ✅ |
| เพิ่มกิจกรรมในไตรมาส 3 | ไปอยู่ไตรมาส 3 ไม่ปนกับไตรมาส 1 (ไตรมาส 1 = 2 · ไตรมาส 2 = 0 · ไตรมาส 3 = 1) ✅ |
| แถบความคืบหน้า | เสร็จ 1 จาก 3 → 33% · แก้เป็นเสร็จอีกอัน → 67% ✅ |
| แก้ไขกิจกรรมและเปลี่ยนสถานะ | บันทึกถูกต้อง แก้ในที่เดิมไม่ต้องเปลี่ยนหน้า ✅ |
| ลำดับกิจกรรม (sortOrder) | นับแยกของใครของมันในแต่ละไตรมาส ✅ |
| หน้าจอมือถือ | เมนูย่อทำงาน แต่ละไตรมาสเรียงลงมา ใช้งานได้ ✅ |

**การเจาะสิทธิ์** (สร้างบัญชีทดสอบ DEPT_USER สังกัด กจร.2 และ EXECUTIVE
แล้วลองยุ่งกับแผนของ กจร.1):

| การทดสอบ | ผล |
|---|---|
| DEPT_USER เห็นตัวชี้วัดกี่รายการในหน้าแผน | 9 รายการ (ของ กจร.2 ครบพอดี) ✅ |
| ใส่ `?dept=` ของส่วนงานอื่นใน URL | ยังเห็น 9 รายการเท่าเดิม ✅ |
| DEPT_USER เปิดแผนของส่วนงานอื่นด้วย URL ตรง | 404 ✅ |
| EXECUTIVE เปิดแผนของส่วนงานอื่น | เปิดดูได้ แต่ไม่มีปุ่มเพิ่ม/แก้/ลบ และขึ้นว่าดูได้อย่างเดียว ✅ |
| **DEPT_USER ยิง Server Action เพิ่ม/แก้/ลบกิจกรรมของส่วนงานอื่น** | ปฏิเสธทั้ง 3 แบบ ✅ |
| **EXECUTIVE ยิง Server Action เพิ่ม/ลบกิจกรรม** | ปฏิเสธทั้ง 2 แบบ ✅ |
| ข้อมูลในฐานข้อมูลหลังการโจมตี 5 ครั้ง | เหมือนเดิมทุกช่อง ไม่มีอะไรถูกเพิ่มหรือลบ ✅ |
| DEPT_USER เพิ่มกิจกรรมในแผน **ของตัวเอง** | ทำได้ปกติ ✅ |

---

### ผลการทดสอบ Phase 7 (ส่วนรายงานผลและคะแนน)

**1. ความถูกต้องของสูตร** (ทดสอบกับเกณฑ์จริงจากฐานข้อมูล):

ใช้ตัวอย่างเดียวกับที่ MOU ระบุ — ค่าใช้จ่ายบริหารต่อไร่ (ค่าน้อยยิ่งดี)
เกณฑ์ ระดับ 1 = 111 · ระดับ 2 = 102.5 · ระดับ 3 = 94 · ระดับ 4 = 85.5 · ระดับ 5 = 77

| ทำได้จริง | คะแนนที่ได้ | ก้าวหน้า |
|---|---|---|
| 120 | 0 (ต่ำกว่าเกณฑ์ระดับ 1) | 78.33% |
| 111 | ระดับ 1 | 84.68% |
| 100 | ระดับ 2 | 94% |
| 94 | ระดับ 3 | 100% |
| **80** | **ระดับ 4** ← ตรงกับตัวอย่างใน MOU | 100% |
| 77 | ระดับ 5 | 100% |

ตัวชี้วัดค่ามากยิ่งดีทดสอบแล้วเช่นกัน · กรณีขอบทดสอบครบ (ยังไม่กรอกผล, ไม่มีเกณฑ์,
ทำได้ 0 ในตัวที่ยิ่งน้อยยิ่งดี, เป้าหมายเป็น 0, ทำได้เกินเป้ามาก)

**2. การใช้งานผ่านหน้าเว็บจริง:**

| การทดสอบ | ผล |
|---|---|
| กรอก 80 แล้วดูตัวอย่างสดบนหน้าจอ | ขึ้น 100% · ระดับ 4 ทันทีที่พิมพ์ ✅ |
| บันทึกร่าง แล้วส่งผล | บันทึกถูกต้อง · ขึ้นชื่อผู้ส่งและเวลาไทย ✅ |
| กดส่งทั้งที่ยังไม่กรอกตัวเลข | ปฏิเสธ พร้อมแนะให้บันทึกร่างไว้ก่อน ✅ |
| ปรับคะแนนด้วยมือโดยไม่ใส่เหตุผล | ปฏิเสธ ✅ |
| ปรับคะแนนด้วยมือพร้อมเหตุผล | บันทึกและขึ้นป้ายเตือนว่าคะแนนถูกปรับ ✅ |
| ดึงรายงานที่ส่งแล้วกลับมาแก้ | สถานะกลับเป็นร่าง ✅ |

**3. การเจาะสิทธิ์:**

| การทดสอบ | ผล |
|---|---|
| DEPT_USER (กจร.2) เปิดหน้ากรอกผลของ กจร.1 | 404 ✅ |
| EXECUTIVE เปิดหน้าเดียวกัน | เปิดดูได้ แต่ไม่มีฟอร์มกรอก ✅ |
| **DEPT_USER ยิง Server Action กรอกผลให้ส่วนงานอื่น** | ปฏิเสธ ✅ |
| **EXECUTIVE ยิง Server Action กรอกผล / ดึงกลับมาแก้** | ปฏิเสธทั้งสองแบบ ✅ |
| ข้อมูลหลังการโจมตี | ไม่เปลี่ยนแปลง ✅ |

### วิธีเก็บไฟล์แนบ (Phase 7b)

**หลักฐานแนบแยกตามระดับคะแนน 1–5** เพราะ MOU กำหนดเงื่อนไขไว้คนละอย่างในแต่ละระดับ
เพิ่มคอลัมน์ `criteriaLevel` ในตาราง `Attachment` แล้ว

**ชนิดและขนาดที่อนุญาต:** PDF · Word · Excel · รูปภาพ (JPG/PNG/WebP) · ไม่เกิน 10 MB ต่อไฟล์
ไม่จำกัดจำนวนไฟล์ ไม่รับไฟล์ที่รันได้และไฟล์บีบอัด เพราะเปิดช่องให้อัปโหลดโปรแกรมอันตราย

**เส้นทางของไฟล์:**

```
เบราว์เซอร์ขอบัตรผ่าน  →  เซิร์ฟเวอร์ตรวจสิทธิ์  →  ออกบัตรผ่าน (จำกัดชนิด+ขนาด)
                                    ↓ ไม่ผ่าน
                              ปฏิเสธตั้งแต่ยังไม่อัปโหลด
        ↓ ผ่าน
เบราว์เซอร์ส่งไฟล์ตรงไป Vercel Blob (ไม่ผ่านเซิร์ฟเวอร์เรา)
        ↓
บอกเซิร์ฟเวอร์ให้บันทึกข้อมูลไฟล์  →  เซิร์ฟเวอร์ถาม Blob เองว่าไฟล์จริงขนาด/ชนิดอะไร
                                        (ไม่เชื่อค่าที่เบราว์เซอร์บอก)
```

จุดที่ตัดสินใจไว้และเหตุผล:

- **ทำไมไม่ส่งไฟล์ผ่านเซิร์ฟเวอร์:** Vercel จำกัดข้อมูลที่ส่งเข้าฟังก์ชันไว้ 4.5 MB
  ไฟล์ 10 MB จะอัปโหลดไม่สำเร็จตอนขึ้นระบบจริง
- **ทำไมตรวจสิทธิ์ตอนออกบัตรผ่าน:** ถ้าไปตรวจหลังอัปโหลดเสร็จ ไฟล์ขึ้นไปอยู่บน Blob แล้ว
  ปฏิเสธทีหลังก็สายเกินไป
- **ทำไมดาวน์โหลดผ่าน `/api/attachments/[id]`:** ไฟล์บน Blob ใครรู้ URL ก็เปิดได้
  ระบบจึงไม่เคยส่ง URL จริงออกไปให้เบราว์เซอร์เห็น แต่ให้กดผ่านระบบเราซึ่งตรวจสิทธิ์ก่อนทุกครั้ง

### ผลการทดสอบ Phase 10 (มือถือ)

ตรวจอัตโนมัติทั้ง 16 หน้าที่จอกว้าง 390 px (ขนาดมือถือที่แคบที่สุดที่ยังพบบ่อย)
ตรวจ 3 อย่างที่พังบ่อยที่สุด: ทั้งหน้าเลื่อนซ้ายขวาได้ · มีของล้นออกนอกจอ · ปุ่มเล็กเกินกว่านิ้วจะกดถูก

| รอบ | ผลตรวจ |
|---|---|
| ก่อนแก้ | มีปัญหา 16 จาก 16 หน้า |
| หลังแก้ | **ผ่านครบ 16 จาก 16 หน้า** |

**เรื่องที่ผ่านตั้งแต่แรก:** ไม่มีหน้าไหนเลยที่ทั้งหน้าเลื่อนซ้ายขวาได้
หรือมีของล้นออกนอกจอ เพราะทุกตารางถูกใส่กรอบให้เลื่อนในตัวเองไว้ตั้งแต่ต้น

**เรื่องที่ต้องแก้ทั้งหมดคือปุ่มเล็กเกินไป** (นิ้วต้องการพื้นที่กดราว 44 px):

| ของที่แก้ | เดิม | หลังแก้ |
|---|---|---|
| ปุ่มเมนูขีดสามขีด · ปุ่มออกจากระบบ | 38 px | 44 px |
| ชิปเลือกไตรมาส / เลือกปีบัญชี | 38 px | 44 px |
| ลิงก์ชื่อตัวชี้วัดในตาราง (ทางเข้าหลักบนมือถือ) | 21 px | 45 px |
| ช่องไตรมาสในตารางรายงานผล (ช่อง "–" ที่กดเพื่อกรอก) | 20 px | 44 px |
| ลิงก์ "← กลับไป..." | 21 px | 44 px |
| ปุ่มแนบไฟล์ · ปุ่มแบ่งหน้า · ปุ่มเล็กอื่นๆ | 34–38 px | 44 px |

**จุดที่เปลี่ยนรูปแบบ ไม่ใช่แค่ขยาย:** ลิงก์ "ดูรายละเอียดตัวชี้วัด" เดิมแทรกอยู่กลางประโยค
ขยายไม่ได้เพราะจะไปทับบรรทัดอื่น จึงย้ายออกมาเป็นปุ่มแยกใต้หัวเรื่องแทน
กดง่ายขึ้นและเห็นชัดขึ้นด้วย

### ผลการทดสอบ Phase 9 (Dashboard)

ทดสอบโดยสร้างรายงานจำลอง 134 รายการ ให้แต่ละส่วนงานส่งผลไม่เท่ากัน
(8 ส่วนงานส่งครบ · 12 ส่วนงานส่งครึ่งเดียว · 10 ส่วนงานยังไม่ส่งเลย) แล้วลบทิ้งหลังทดสอบ

| การทดสอบ | ผล |
|---|---|
| ตัวเลขสรุป | 301 ตัวชี้วัด · ส่งแล้ว 45% (134) · คะแนนเฉลี่ย 1.80 · ยังไม่ส่งเลย 10 ส่วนงาน ✅ |
| ตารางเปรียบเทียบส่วนงาน | 30 แถว เรียงตามคะแนนถูกต้อง (อันดับ 1 = กจส.2 ส่งครบ 10/10 คะแนน 2.83) ✅ |
| สรุปตามมิติ | 6 มิติ · สัดส่วนน้ำหนักรวมกันได้ 100% พอดี ✅ |
| กรองไตรมาสที่ยังไม่มีข้อมูล | แสดง 0% และ "ยังไม่มีผล" ไม่พัง ✅ |
| ดาวน์โหลด Excel | ได้ไฟล์ `สรุปผล MOU 2569 ล่าสุด.xlsx` 3 แผ่นงาน ภาษาไทยไม่เพี้ยน ✅ |
| ตัวเลขในไฟล์ Excel เทียบกับหน้าจอ | ตรงกันทุกช่อง (ใช้ฟังก์ชันคำนวณตัวเดียวกัน) ✅ |
| หน้าจอมือถือ | ใช้งานได้ ตารางเลื่อนแนวนอนในกรอบตัวเอง ✅ |

**จุดที่แก้ระหว่างทดสอบ:** เดิมแสดง "น้ำหนักรวม" ของแต่ละมิติเป็นผลรวมดิบ
ซึ่งพอรวมข้าม 30 ส่วนงานได้ตัวเลขอย่าง 835% อ่านแล้วสับสน
เปลี่ยนเป็น "สัดส่วนน้ำหนัก" ที่รวมกันได้ 100% แทน

**หลักการที่ใช้ตอนออกแบบกราฟ:**

- แถบคะแนนใช้ **สีเดียวทุกแถบ** ไม่ไล่สีตามคะแนน เพราะความยาวแถบบอกคะแนนอยู่แล้ว
  ถ้าไล่สีด้วยจะเป็นการบอกเรื่องเดิมซ้ำสองทาง และทำให้แถบที่คะแนนใกล้กันแยกจากกันยากขึ้น
- ทุกที่ที่แสดงคะแนนรวม จะแสดง **"ส่งผลแล้วกี่ %" คู่กันเสมอ**
  เพราะส่วนงานที่ส่งไม่ครบจะได้คะแนนรวมน้อยกว่าโดยธรรมชาติ
  ถ้าไม่บอกไว้ ผู้อ่านจะเข้าใจผิดว่าหน่วยนั้นทำงานได้แย่กว่า

### ผลการทดสอบ Phase 8 (ตั้งเวลาเปิด-ปิดระบบ)

ทดสอบด้วยบัญชี DEPT_USER สังกัด กจร.1 โดย **ยิง Server Action ตรง** ทุกครั้ง
ไม่ได้กดผ่านหน้าเว็บ เพราะปุ่มถูกซ่อนอยู่แล้วเมื่อปิดรับ

| สถานการณ์ | ส่วนงานบันทึกได้ไหม | ผล |
|---|---|---|
| เลยเวลาปิดแล้ว | ไม่ได้ | ปฏิเสธ "ปิดรับข้อมูลแล้วเมื่อ 31 ม.ค. 2569 23:59 น." ✅ |
| ช่วงเวลาเปิดอยู่ | ได้ | บันทึก 85 → คะแนนระดับ 4 ✅ |
| ส่วนกลางสั่งปิดฉุกเฉิน | ไม่ได้ | ปฏิเสธ "ส่วนกลางปิดรับข้อมูลของไตรมาสนี้ไว้" ✅ |
| เลยเวลาปิด + ได้ขยายเวลาเฉพาะหน่วย | ได้ | บันทึกผ่าน · หน้าจอขึ้นว่าได้รับการขยายเวลา ✅ |
| ยกเลิกการขยายเวลาแล้ว | ไม่ได้ | ปฏิเสธอีกครั้ง ✅ |
| **ส่วนกลาง (ADMIN) ตอนช่วงเวลาปิด** | ได้ | บันทึกผ่าน ตามที่ออกแบบไว้ ✅ |

**การกันไม่ให้ส่วนงานไปยุ่งกับการตั้งเวลาเอง:**

| การทดสอบ | ผล |
|---|---|
| DEPT_USER ยิง Server Action แก้ช่วงเวลาเปิด-ปิด | ปฏิเสธ ✅ |
| DEPT_USER ยิง Server Action ขยายเวลาให้ตัวเอง | ปฏิเสธ ✅ |
| DEPT_USER ยิง Server Action ยกเลิกการขยายเวลา | ปฏิเสธ ✅ |

**การตรวจความสมเหตุสมผลของค่าที่กรอก:**

| การทดสอบ | ผล |
|---|---|
| ตั้งเวลาปิดก่อนเวลาเปิด | ปฏิเสธ ✅ |
| ขยายเวลาให้ปิด *เร็วกว่า* เดิม | ปฏิเสธ (ขยายได้อย่างเดียว ไม่ให้ใช้ย่นเวลาลงโทษเฉพาะหน่วย) ✅ |
| ขยายเวลาโดยไม่ใส่เหตุผล | ปฏิเสธ ✅ |

> **ข้อที่ตัดสินใจไว้:** ส่วนกลางบันทึกข้อมูลได้เสมอแม้เลยเวลาปิด
> เพราะเป็นคนคุมการเปิด-ปิดเอง และต้องแก้ข้อมูลให้ส่วนงานได้เมื่อมีข้อผิดพลาด
> หน้าจอจะขึ้นเตือนให้เห็นชัดว่ากำลังแก้ในช่วงที่ปิดรับอยู่
> ถ้าต้องการให้ล็อกส่วนกลางด้วย บอกได้ แก้ที่ `src/lib/submission-window.ts` จุดเดียว

### บั๊กที่เจอและแก้แล้วระหว่าง Phase 7

**ค่าในช่องตัวเลือก (`<select>`) หายเงียบๆ หลังกดบันทึกแล้วไม่ผ่าน**

React จะล้างค่าในฟอร์มทุกครั้งที่ Server Action ตอบกลับ ช่องพิมพ์ข้อความถูกเติมค่ากลับให้เอง
แต่ช่องตัวเลือกไม่ถูกเติมกลับ ทำให้หน้าจอแสดงค่าหนึ่ง แต่ตอนกดบันทึกครั้งถัดไปกลับส่งอีกค่าหนึ่งไป

พบตอนทดสอบการปรับคะแนนด้วยมือ: เลือกระดับ 5 → กดส่งแล้วไม่ผ่านเพราะลืมใส่เหตุผล →
ใส่เหตุผลแล้วกดส่งใหม่ → ระบบบันทึกคะแนนที่คำนวณอัตโนมัติ (ระดับ 2) แทนระดับ 5 ที่เลือกไว้

แก้แล้วโดยเติมค่ากลับเองทุกครั้งที่ action ตอบกลับ และไล่แก้ที่อื่นที่มีปัญหาแบบเดียวกันด้วย:

| ไฟล์ | ช่องที่ได้รับผลกระทบ | ถ้าไม่แก้จะเกิดอะไร |
|---|---|---|
| ฟอร์มกรอกผล | คะแนนที่ปรับด้วยมือ | บันทึกคะแนนผิดจากที่เห็นบนหน้าจอ |
| ฟอร์มผู้ใช้ | สิทธิ์การใช้งาน | สร้างบัญชีด้วยสิทธิ์ผิด |
| ฟอร์มตัวชี้วัด | ทิศทาง (ค่ามาก/น้อยดี) | คะแนนของตัวชี้วัดนั้นกลับหัวทั้งหมด |

---

### เรื่องเน็ตกับพอร์ต 5432 (แก้ถาวรแล้ว)

เคยติดปัญหาซ้ำ 3 ครั้งว่ารันเว็บบนเครื่องแล้ว login ไม่ได้ ขึ้นว่าต่อฐานข้อมูลไม่ติด
สาเหตุคือ **เครือข่ายบล็อกพอร์ต 5432** ที่ใช้ต่อ PostgreSQL แบบปกติ

**แก้ถาวรแล้ว (7 ก.ย. 2569):** เปลี่ยนไปต่อผ่าน `@prisma/adapter-neon`
ซึ่งวิ่งบน **HTTPS พอร์ต 443** แทน ตอนนี้ต่อฐานข้อมูลได้ทุกเครือข่าย
และเป็นวิธีที่เหมาะกับ Vercel อยู่แล้ว เพราะฟังก์ชันบน Vercel เกิดใหม่ตลอด
การต่อแบบเดิมจะเปิดการเชื่อมต่อค้างไว้จนฐานข้อมูลปฏิเสธ

> **ข้อควรรู้:** คำสั่ง `prisma migrate` และ `prisma db push` ยังใช้พอร์ต 5432 อยู่
> ถ้าต้องแก้โครงฐานข้อมูล ต้องอยู่บนเน็ตที่ไม่บล็อกพอร์ตนั้น
> ส่วนการใช้งานเว็บตามปกติ ใช้ได้ทุกเน็ตแล้ว

### ติดอยู่ที่อะไรตอนนี้

**รอ token ของ Vercel Blob** จึงจะทดสอบส่วนไฟล์แนบได้ โค้ดเขียนเสร็จและ build ผ่านแล้ว

สิ่งที่ต้องทำ (ทำครั้งเดียว):

1. เข้า vercel.com → เมนู **Storage** → **Create Database** → เลือก **Blob**
2. เปิด store ที่สร้าง → หาค่า `BLOB_READ_WRITE_TOKEN` (ขึ้นต้นด้วย `vercel_blob_rw_`)
3. เพิ่มบรรทัดนี้ในไฟล์ `.env` แล้วรัน `npm run dev` ใหม่

```
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxx
```

> ตอน deploy ขึ้น Vercel จริง ไม่ต้องตั้งค่านี้เอง Vercel จะใส่ให้อัตโนมัติ
> ถ้าผูก Blob store เข้ากับโปรเจกต์แล้ว

**สิ่งที่ต้องทำก่อนใช้งานจริง:** เปลี่ยนรหัสผ่านบัญชี ADMIN ที่หน้า `/account`
และสร้างบัญชีให้ผู้รับผิดชอบแต่ละส่วนงานที่หน้า `/admin/users`

---

## ข้อสรุปการตัดสินใจ (ยืนยันแล้ว)

| หัวข้อ | ข้อสรุป | ผลต่อการออกแบบ |
|---|---|---|
| **ใครสร้างตัวชี้วัด** | ส่วนกลางสร้างให้ ส่วนงานห้ามแก้ไข | ปุ่มเพิ่ม/แก้ตัวชี้วัดมีเฉพาะ ADMIN |
| **ขั้นตอนอนุมัติ** | ไม่มี — ส่วนงานบันทึกแล้วจบ | `ReportStatus` มีแค่ `DRAFT` / `SUBMITTED` |
| **การมองเห็นข้อมูล** | แต่ละส่วนงานเห็นแค่ของตัวเอง | ทุก query ของ `DEPT_USER` กรองด้วย `departmentId` ที่เซิร์ฟเวอร์ |
| **วิธี Login** | บัญชีในระบบเอง (อีเมล + รหัสผ่าน) | Auth.js + bcrypt — ADMIN สร้างบัญชีให้ ไม่มีการสมัครเอง |
| **ฐานข้อมูล** | Neon (PostgreSQL บนคลาวด์) | ไม่ต้องติดตั้งอะไรในเครื่อง |
| **การนำขึ้นระบบ** | **Vercel** | ไม่ต้องดูแลเซิร์ฟเวอร์เอง — แต่มีข้อจำกัดเรื่องไฟล์แนบ (ดูหัวข้อ Vercel) |
| **การจัดประเภทหน่วยงาน** | ไม่สังกัดส่วนงาน = กจร. 5 + กจส. 3 + นธก. + หกป. รวม 10 หน่วย | ที่เหลือ 20 หน่วยเป็นส่วนงาน |

---

## ผู้ใช้งานและสิทธิ์ (Role)

| Role | ใคร | ทำอะไรได้ |
|---|---|---|
| `ADMIN` | ส่วนกลาง | จัดการทุกอย่าง: ปีบัญชี, ตัวชี้วัด, ผู้ใช้, ตั้งเวลาเปิด-ปิดระบบ, เห็นทุกส่วนงาน |
| `DEPT_USER` | ผู้รับผิดชอบส่วนงาน | ดูตัวชี้วัด **ของส่วนงานตัวเองเท่านั้น** (แก้ไม่ได้), บันทึกแผน, กรอกผล, แนบไฟล์ |
| `EXECUTIVE` | ผู้บริหาร | ดูได้ทุกส่วนงาน แต่แก้ไขอะไรไม่ได้ |

> **หลักสำคัญ:** การจำกัดสิทธิ์ต้องตรวจที่ฝั่งเซิร์ฟเวอร์เสมอ
> การซ่อนปุ่มบนหน้าจอคือ "ความสวยงาม" ส่วนการตรวจที่เซิร์ฟเวอร์คือ "ความปลอดภัย"
> กฎทั้งหมดรวมอยู่ใน `src/lib/permissions.ts` ไฟล์เดียว

---

## เทคโนโลยีที่ใช้

| เทคโนโลยี | หน้าที่ | อธิบายแบบเข้าใจง่าย |
|---|---|---|
| **Next.js 15** | Framework หลัก | เป็นทั้งหน้าเว็บและเซิร์ฟเวอร์ในโปรเจกต์เดียว |
| **React 19** | สร้างหน้าจอ | เขียน UI เป็นชิ้นๆ (Component) แล้วประกอบกัน |
| **TypeScript 5** | ภาษาที่ใช้เขียน | JavaScript + ตรวจชนิดข้อมูล ดักพิมพ์ผิดก่อนรัน |
| **Tailwind CSS 4** | ตกแต่งหน้าจอ | จัดหน้าตาและทำ Responsive มือถือได้ง่าย |
| **Prisma 6** | ตัวเชื่อมกับฐานข้อมูล | เขียน `db.indicator.findMany()` แทน SQL |
| **PostgreSQL (Neon)** | ฐานข้อมูล | เก็บข้อมูลจริงทั้งหมด อยู่บนคลาวด์ |
| **Auth.js v5** | ระบบ Login | จัดการ login / session / จำว่าใครเป็นใคร |
| **bcryptjs** | เข้ารหัสรหัสผ่าน | เก็บรหัสผ่านแบบที่ย้อนกลับไม่ได้ |
| **Zod** | ตรวจข้อมูลที่ผู้ใช้กรอก | เช็คก่อนบันทึกลงฐานข้อมูล |
| **Vercel** | ที่รันระบบจริง | เชื่อมกับ GitHub แล้ว deploy อัตโนมัติทุกครั้งที่แก้โค้ด |

### ยังไม่ได้ติดตั้ง (จะเพิ่มเมื่อถึง Phase ที่ใช้)

| เทคโนโลยี | ใช้ทำอะไร | Phase |
|---|---|---|
| **@vercel/blob** | เก็บไฟล์แนบ (จำเป็นบน Vercel) | 7 |
| **Recharts** | กราฟใน Dashboard | 9 |
| **ExcelJS** | ส่งออกรายงานเป็น .xlsx | 9 |

---

## การนำขึ้นระบบจริงบน Vercel

### ทำไมต้องระวังเป็นพิเศษ

Vercel ไม่ใช่เซิร์ฟเวอร์แบบเดิมที่มีฮาร์ดดิสก์ถาวร แต่เป็นแบบ **Serverless**
คือโค้ดจะถูกปลุกขึ้นมาทำงานเป็นครั้งๆ แล้วดับไป

**ผลที่ตามมาที่สำคัญที่สุด: เขียนไฟล์ลงดิสก์แล้วไฟล์จะหาย**

| ประเด็น | บนเซิร์ฟเวอร์ทั่วไป | บน Vercel |
|---|---|---|
| เก็บไฟล์แนบในโฟลเดอร์ `uploads/` | ทำได้ | **ทำไม่ได้ ไฟล์จะหาย** |
| ขนาดไฟล์ที่อัปโหลดผ่านฟอร์มได้ | ตั้งเองได้ | จำกัดประมาณ 4.5 MB |
| เวลาของเซิร์ฟเวอร์ | ตั้งเป็นไทยได้ | เป็น UTC เสมอ |

### สิ่งที่ต้องทำต่างจากแผนเดิม

**1. ไฟล์แนบต้องเก็บบน Vercel Blob (ไม่ใช่โฟลเดอร์ `uploads/`)**

```
เดิม (ใช้ไม่ได้บน Vercel)          →   ใหม่
─────────────────────────              ──────────────────────────
บันทึกไฟล์ลง ./uploads/xxx.pdf         อัปโหลดขึ้น Vercel Blob
เก็บ path ในฐานข้อมูล                  เก็บ URL ที่ได้กลับมาในฐานข้อมูล
```

ตาราง `Attachment` มีฟิลด์ `storagePath` อยู่แล้ว เปลี่ยนไปเก็บ URL ของ Blob แทน
ไม่ต้องแก้โครงฐานข้อมูล

**2. ไฟล์ใหญ่เกิน 4.5 MB ต้องอัปโหลดตรงจากเบราว์เซอร์ไป Blob**

ไม่ส่งผ่านเซิร์ฟเวอร์ของเรา (Vercel Blob รองรับวิธีนี้ เรียกว่า client upload)
เซิร์ฟเวอร์ทำหน้าที่แค่ออกบัตรผ่านให้ และ **ตรวจว่าระบบยังเปิดรับอยู่หรือไม่ก่อนออกบัตร**

**3. Prisma ต้องสั่ง generate ตอน build**

Vercel ใช้แคชของ dependency ทำให้ Prisma Client ไม่ถูกสร้างใหม่อัตโนมัติ
ต้องแก้ `package.json`:

```json
"build": "prisma generate && next build"
```

**4. เวลาปิดระบบต้องแปลง Timezone เสมอ**

เซิร์ฟเวอร์ Vercel เป็น UTC ถ้าลืมแปลง ระบบจะปิดคลาดเคลื่อนไป 7 ชั่วโมง
เก็บเป็น UTC ในฐานข้อมูล แล้วแปลงเป็น `Asia/Bangkok` ตอนแสดงผลเท่านั้น

### ขั้นตอน Deploy (ทำครั้งเดียวตอน Phase 11)

1. push โค้ดขึ้น GitHub
2. เข้า vercel.com → Import Project → เลือก repo
3. ตั้งค่า Environment Variables ในหน้า Vercel (ห้ามใส่ในโค้ด):
   `DATABASE_URL`, `AUTH_SECRET`, `BLOB_READ_WRITE_TOKEN`
4. ตั้ง Region เป็น **Singapore (sin1)** เพื่อให้เร็วสำหรับผู้ใช้ในไทย
5. รัน `npx prisma migrate deploy` ครั้งแรกเพื่อสร้างตารางบนฐานข้อมูลจริง
6. หลังจากนี้ ทุกครั้งที่ push โค้ดขึ้น GitHub Vercel จะ deploy ให้อัตโนมัติ

### ค่าใช้จ่าย

แผน Hobby ของ Vercel และแผนฟรีของ Neon เพียงพอสำหรับผู้ใช้ 30 หน่วยงาน
หากใช้งานภายในองค์กร ควรตรวจสอบเงื่อนไขการใช้เชิงพาณิชย์ของแผน Hobby อีกครั้ง

---

## ข้อมูลจริงจาก MOU (ดึงครบแล้ว)

### แหล่งข้อมูล

| โฟลเดอร์ | เนื้อหา | ใช้ทำอะไร |
|---|---|---|
| `MOU 69 ฉบับลงนาม/` | PDF ฉบับลงนาม 30 ไฟล์ (สแกน) | ยืนยันชื่อหน่วยงานและวันที่ |
| `mou 69 ฉบับลงนาม (word)/` | Word 30 ไฟล์ | **ดึงตัวชี้วัดทั้งหมดจากที่นี่** |
| `ผลการดำเนินงาน/` | รายงานผลไตรมาส 2–3 | ตัวอย่างรูปแบบรายงาน (ยังไม่ได้ใช้) |

### โครงสร้างตารางตัวชี้วัดใน MOU

```
| เกณฑ์วัดการดำเนินงาน | หน่วยวัด | Base line 2568 | น้ำหนัก (%) | ค่าเกณฑ์วัดปีบัญชี 2569 | การปรับค่าเกณฑ์วัด |
|                      |          |                |             |  1  |  2  |  3  |  4  |  5  |                   |
```

ตัวอย่างจริง (มีเหมือนกันทุกส่วนงาน):

| ตัวชี้วัด | หน่วย | Base line | น้ำหนัก | ระดับ 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|---|---|
| ค่าใช้จ่ายบริหารต่อไร่ (PA) | บาท/ไร่ | 111.14 | 10% | 111.00 | 102.50 | 94.00 | 85.50 | **77.00** |

### สิ่งที่พบและมีผลต่อการออกแบบ

| สิ่งที่พบ | ผลต่อระบบ |
|---|---|
| ตัวชี้วัดจัดกลุ่มตาม **มิติ** (5 มิติ) | เพิ่มฟิลด์ `dimension` และ Dashboard สรุปตามมิติ |
| เกณฑ์คะแนนเป็น **ค่าเดี่ยวต่อระดับ** ไม่ใช่ช่วง | `ScoreCriteria.targetValue` |
| **29 ตัวชี้วัดค่าน้อยยิ่งดี** (ค่าใช้จ่ายต่อไร่) | เพิ่ม `direction` — ถ้าไม่มี ระบบจะให้คะแนนกลับด้าน |
| มีตัวชี้วัดย่อย เช่น 4.1, 4.2 ใต้หัวข้อ 4 | เพิ่ม `groupName` |
| มี Base line ปีก่อน | เพิ่ม `baselineValue` |
| ค่าเป้าหมายหลัก = ระดับ 3 | `Indicator.targetValue` ใช้ค่าระดับ 3 |

### มิติที่พบ (301 ตัวชี้วัด)

| มิติ | จำนวน |
|---|---|
| มิติด้านการเงิน | 98 |
| มิติด้านกระบวนการภายใน | 70 |
| มิติด้านการบริหารงานภายใน | 52 |
| มิติด้านผู้มีส่วนได้ส่วนเสีย | 46 |
| มิติด้านการเรียนรู้และเติบโต | 33 |
| มิติด้านการบริหารจัดการผู้มีส่วนได้ส่วนเสีย | 2 |

### วิธีตรวจสอบความถูกต้อง

ใช้ **ผลรวมน้ำหนักต้องเท่ากับ 100%** เป็นตัวตรวจ ทำให้พบรูปแบบที่ผิดปกติ 3 แบบ
และแก้จนได้ครบ 100% ทั้ง 30 ฉบับ:

1. ยัดตัวชี้วัดหลายตัวไว้ในช่องตารางเดียว (ฝสผ. ข้อ 3.2–3.6)
2. รวมหัวข้อมิติกับตัวชี้วัดไว้ในช่องเดียว (ฝพก. ข้อ 7)
3. ไม่เว้นวรรคหลังเลขข้อ เช่น "7.การสร้าง..." (สผว. ข้อ 7)

### จุดที่คัดลอกตามต้นฉบับ (ยังไม่ได้แก้)

- กจร.1 ระบุหน่วยของ "ค่าใช้จ่ายบริหารต่อไร่" เป็น **"ร้อยละ"** ขณะที่อีก 29 ฉบับใช้ "บาท/ไร่"
- มี 1 รายการใช้หน่วย **"เฟคเตอร์"** (น่าจะพิมพ์คลาดจาก "แฟคเตอร์")
- รหัสเขตอีสานตอนบน: PDF สะกด `กบท.ข.อนบ.` แต่ Word และอีก 6 เขตใช้ `กยท.ข.` → **ยึดตาม Word**

---

## รายชื่อ 30 หน่วยงาน

### หน่วยงานที่ไม่สังกัดส่วนงาน (10)

| รหัส | ชื่อ |
|---|---|
| กจร.1, กจร.2, กจร.4, กจร.5, กจร.6 | กองจัดการโรงงาน 1, 2, 4, 5, 6 (ไม่มีโรงงาน 3) |
| กจส.1, กจส.2, กจส.3 | กองจัดการสวนยาง 1, 2, 3 |
| นธก. | หน่วยธุรกิจ |
| หกป. | หน่วยกำกับดูแลการปฏิบัติตามกฎเกณฑ์ |

### ส่วนงาน (20)

| กลุ่ม | รหัสและชื่อ |
|---|---|
| **เขต (7)** | กยท.ข.น. เขตภาคเหนือ · กยท.ข.กอ. เขตภาคกลางและภาคตะวันออก · กยท.ข.อนบ. เขตภาคตะวันออกเฉียงเหนือตอนบน · กยท.ข.อนล. เขตภาคตะวันออกเฉียงเหนือตอนล่าง · กยท.ข.ตบ. เขตภาคใต้ตอนบน · กยท.ข.ตก. เขตภาคใต้ตอนกลาง · กยท.ข.ตล. เขตภาคใต้ตอนล่าง |
| **ฝ่าย (10)** | ฝกค. การคลัง · ฝกม. กฎหมาย · ฝทม. ทรัพยากรมนุษย์ · ฝทส. เทคโนโลยีสารสนเทศ · ฝบท. บริหารทรัพย์สิน · ฝพก. พัฒนาเกษตรกรและสถาบันเกษตรกร · ฝยศ. ยุทธศาสตร์องค์กร · ฝศย. เศรษฐกิจยาง · ฝสผ. ส่งเสริมและพัฒนาการผลิต · ฝอย. อุตสาหกรรมยาง |
| **สำนัก/สถาบัน (3)** | สผว. สำนักผู้ว่าการ · สวย. สถาบันวิจัยยาง · สตส. สำนักตรวจสอบภายใน |

---

## โครงสร้างฐานข้อมูล

10 ตาราง ดูรายละเอียดพร้อมคำอธิบายทุกฟิลด์ได้ที่ `prisma/schema.prisma`

```
Department (ส่วนงาน 30)          FiscalYear (ปีบัญชี)
├─ code, name, type              ├─ year, startDate, endDate, isActive
└─ sortOrder, isActive

User (ผู้ใช้)                     Indicator (ตัวชี้วัด)  ★ ตารางหลัก
├─ email, name, passwordHash     ├─ fiscalYearId, departmentId
├─ role                          ├─ code, name, description
└─ departmentId                  ├─ dimension (มิติ), groupName
                                 ├─ unit, targetValue, baselineValue, weight
                                 ├─ direction (ค่ามาก/น้อยดี)
                                 └─ adjustmentNote, status

ScoreCriteria (เกณฑ์คะแนน 1-5)   ActionPlan (แผนดำเนินงาน)
├─ indicatorId, level            ├─ indicatorId, quarter
├─ targetValue  ← MOU ใช้แบบนี้   ├─ activity, expectedOutput
└─ minValue, maxValue (สำรอง)    └─ status, sortOrder

QuarterlyReport (ผลรายไตรมาส)  ★ ตารางหลัก
├─ indicatorId, quarter
├─ actualValue      ← ส่วนงานกรอกเอง
├─ progressPct      ← คำนวณอัตโนมัติ
├─ scoreLevel       ← เทียบจาก ScoreCriteria
├─ scoreOverridden, scoreNote, narrative
├─ status, submittedAt, submittedById
└─ unique(indicatorId + quarter)

Attachment (ไฟล์แนบ)             SubmissionWindow (ช่วงเปิด-ปิดระบบ)
├─ reportId                      ├─ fiscalYearId, quarter
├─ originalName                  ├─ openAt, closeAt   ← ข้อ 9
├─ storagePath ← URL ของ Blob    └─ isForceClosed
└─ mimeType, sizeBytes

WindowException (ขยายเวลาเฉพาะหน่วย)   AuditLog (ประวัติการแก้ไข)
├─ windowId, departmentId              ├─ userId, action
└─ closeAt, reason                     └─ entity, entityId, detail
```

---

## โครงสร้างโปรเจกต์ (ตามที่สร้างจริง)

```
ระบบ mou/
├─ prisma/
│  ├─ schema.prisma          ★ พิมพ์เขียวฐานข้อมูล
│  ├─ departments.ts         รายชื่อ 30 หน่วยงาน
│  ├─ indicators.json        ★ ตัวชี้วัด 301 รายการจาก MOU
│  └─ seed.ts                สคริปต์ใส่ข้อมูลตั้งต้น
│
├─ src/
│  ├─ auth.ts                ★ ตั้งค่าระบบ Login
│  ├─ app/
│  │  ├─ page.tsx            หน้าแรก (สาธารณะ) แสดงสถานะติดตั้ง
│  │  ├─ login/              หน้า Login
│  │  ├─ (main)/             ★ ทุกหน้าในนี้ต้อง login ก่อน
│  │  │  ├─ layout.tsx       เมนูตาม role + เมนูมือถือ
│  │  │  ├─ dashboard/       ข้อ 7
│  │  │  ├─ indicators/      ข้อ 1, 2, 3
│  │  │  ├─ reports/         ★ ข้อ 4, 5 กรอกผลและคะแนนรายไตรมาส
│  │  │  ├─ plans/           ★ ข้อ 6 วางแผนกิจกรรมรายไตรมาส
│  │  │  └─ admin/           ★ เฉพาะ ADMIN
│  │  │     ├─ users/        จัดการบัญชีผู้ใช้ 30 ส่วนงาน
│  │  │     ├─ fiscal-years/ ปีบัญชี + คัดลอกตัวชี้วัดข้ามปี
│  │  │     └─ windows/      ★ ข้อ 9 ตั้งเวลาเปิด-ปิด + ขยายเวลาเฉพาะหน่วย
│  │  └─ api/
│  │     ├─ auth/            จุดรับ-ส่งของระบบ Login
│  │     ├─ attachments/     ออกบัตรผ่านอัปโหลด + ดาวน์โหลดแบบตรวจสิทธิ์
│  │     └─ export/          สร้างไฟล์ Excel สรุปผล
│  │
│  ├─ lib/
│  │  ├─ db.ts               ตัวเชื่อมฐานข้อมูล
│  │  ├─ permissions.ts      ★ กฎสิทธิ์ทั้งหมดรวมที่เดียว
│  │  ├─ session.ts          requireUser() / requireAdmin()
│  │  ├─ validation.ts       กฎตรวจข้อมูลที่กรอก (Zod)
│  │  ├─ audit.ts            บันทึกว่าใครทำอะไรเมื่อไหร่
│  │  ├─ datetime.ts         ★ แปลงเวลาไทย ↔ UTC ที่เดียว
│  │  ├─ plan.ts             ข้อความและตัวช่วยของแผนงาน
│  │  ├─ scoring.ts          ★ คิด % ความก้าวหน้าและคะแนน 1-5
│  │  ├─ attachments.ts      กฎเรื่องไฟล์แนบ (ชนิด/ขนาด/ตรวจ URL)
│  │  ├─ submission-window.ts ★ ตรวจช่วงเวลาเปิด-ปิด รวมที่เดียว
│  │  └─ dashboard.ts        ★ รวมตัวเลขสรุป ใช้ร่วมกับไฟล์ Excel
│  │
│  ├─ actions/               Server Action (ตรวจสิทธิ์ทุกฟังก์ชัน)
│  │  ├─ auth.ts             login / logout
│  │  ├─ account.ts          เปลี่ยนรหัสผ่านตัวเอง
│  │  ├─ indicators.ts       เพิ่ม / แก้ / เก็บเข้าคลังตัวชี้วัด
│  │  ├─ users.ts            สร้าง / แก้ / ตั้งรหัสผ่านใหม่ให้ผู้ใช้
│  │  ├─ fiscal-years.ts     เพิ่มปี / สลับปี / คัดลอกตัวชี้วัด
│  │  ├─ plans.ts            เพิ่ม / แก้ / ลบกิจกรรมในแผน
│  │  ├─ reports.ts          บันทึกร่าง / ส่งผล / ดึงกลับมาแก้
│  │  ├─ attachments.ts      บันทึกข้อมูลไฟล์ / ลบไฟล์
│  │  └─ windows.ts          ตั้งเวลาเปิด-ปิด / ขยายเวลาเฉพาะหน่วย
│  └─ types/next-auth.d.ts   ขยาย type ของ session
│
├─ .env                      ค่าลับ (ห้าม commit)
├─ .env.example              ตัวอย่างค่าที่ต้องตั้ง
└─ plan.md                   ★ เอกสารนี้
```

**หลักคิด:** ตรรกะสำคัญ 3 อย่าง (สิทธิ์ / คะแนน / เปิด-ปิดระบบ) แยกไว้ใน `src/lib/` ที่เดียว
เพราะถูกเรียกจากหลายหน้า ถ้าเขียนซ้ำกระจายไป จะแก้ไม่ทั่วและเกิดช่องโหว่

---

## ตรรกะสำคัญ 3 เรื่อง

### 1. % ความก้าวหน้า (ข้อ 4)

```
ค่ามากยิ่งดี : progressPct = (ผลงานจริง ÷ เป้าหมาย) × 100     จำกัดไม่เกิน 100
ค่าน้อยยิ่งดี: progressPct = (เป้าหมาย ÷ ผลงานจริง) × 100     จำกัดไม่เกิน 100
```

> **แก้จากแผนเดิม (7 ก.ย. 2569):** แผนเดิมเขียนสูตรเดียวคือ ผลงานจริง ÷ เป้าหมาย
> ซึ่งใช้กับตัวชี้วัดที่ **ค่าน้อยยิ่งดี** ไม่ได้ (มี 29 ตัวชี้วัดต่อปี)
>
> ตัวอย่าง: ค่าใช้จ่ายบริหารต่อไร่ เป้าหมาย 94 บาท ทำได้จริง 80 บาท
> ถือว่าทำได้ **ดีกว่า** เป้าหมาย ควรได้เกิน 100% แต่สูตรเดิมให้ 80 ÷ 94 = 85%
> ซึ่งอ่านผิดความหมายว่าทำได้ไม่ถึงเป้า จึงต้องกลับเศษกับส่วนสำหรับตัวชี้วัดกลุ่มนี้

> **ข้อจำกัดที่ต้องรู้:** ระบบ **อ่านตัวเลขจากในไฟล์แนบเองไม่ได้** (เป็น PDF/Word/สแกน)
> แบบฟอร์มรายไตรมาสจึงต้องให้ส่วนงาน **กรอกตัวเลขผลงานควบคู่กับการแนบไฟล์**
> ไฟล์แนบทำหน้าที่เป็น "หลักฐานประกอบ" ระบบคำนวณ % จากตัวเลขที่กรอก

### 2. คะแนน 1–5 (ข้อ 5)

เทียบผลงานจริงกับ `ScoreCriteria` ของตัวชี้วัดนั้น โดย **ต้องดู `direction` ด้วยเสมอ**

```
ตัวอย่าง: ค่าใช้จ่ายบริหารต่อไร่ (LOWER_IS_BETTER)
  ระดับ 1 = 111.00 | ระดับ 3 = 94.00 | ระดับ 5 = 77.00
  ทำได้จริง 80.00 บาท/ไร่  →  ได้คะแนนระดับ 4
```

ระบบเทียบให้อัตโนมัติ แต่เปิดให้ผู้ประเมินปรับด้วยมือได้ (บันทึกใน `scoreOverridden` + `scoreNote`)

**คะแนนรวมของส่วนงาน** = ผลรวมของ (คะแนนตัวชี้วัด × น้ำหนัก) ทุกตัวชี้วัด

### 3. ตั้งเวลาปิดระบบ (ข้อ 9)

```
ผู้ใช้กดแนบไฟล์ / บันทึกผล
        ↓
เซิร์ฟเวอร์ตรวจ SubmissionWindow: เวลาปัจจุบันอยู่ในช่วง openAt–closeAt หรือไม่
        ↓                              ↓
      อยู่ในช่วง                    เลยเวลา / ปิดฉุกเฉิน
        ↓                              ↓
     บันทึกได้                  ปฏิเสธ + แจ้ง "ระบบปิดรับข้อมูลแล้ว"
```

จุดที่ต้องระวัง:

- ❌ ซ่อนแค่ปุ่มบนหน้าจอ → คนที่รู้วิธียังยิงข้อมูลเข้ามาได้
- ✅ **ตรวจที่เซิร์ฟเวอร์ทุกครั้งก่อนบันทึก** แล้วค่อยซ่อนปุ่มเพิ่มเพื่อความสวยงาม
- ✅ กรณีอัปโหลดตรงไป Blob ต้องตรวจ **ตอนออกบัตรผ่าน** ไม่ใช่หลังอัปโหลดเสร็จ
- ⏰ **Timezone** — Vercel เป็น UTC เก็บ UTC แสดงผลเป็น `Asia/Bangkok`
- มีฟังก์ชันขยายเวลาเฉพาะส่วนงาน (`WindowException`) เผื่อกรณีขอผ่อนผัน

---

## ขั้นตอนการพัฒนาที่เหลือ

| Phase | งาน | ผลลัพธ์ที่จับต้องได้ |
|---|---|---|
| **7b** | ไฟล์แนบหลักฐาน (รอ token จาก Vercel Blob) | แนบไฟล์ประกอบรายงานได้ |
| **11** | ทดสอบกับผู้ใช้จริง + Deploy ขึ้น Vercel | ระบบใช้งานจริง |

### หลักการระหว่างพัฒนา

1. **ทำให้ใช้งานได้ก่อน แล้วค่อยทำให้สวย** — Phase 4–8 คือหัวใจ, 9–10 คือการขัดเกลา
2. **ทำทีละ Phase ให้จบ** แต่ละ Phase ต่อยอดจากอันก่อน
3. **Commit git ทุกครั้งที่ทำสำเร็จ 1 อย่าง** พังเมื่อไหร่ย้อนกลับได้
4. **ให้ผู้ใช้จริง 1–2 ส่วนงานลองใช้ตั้งแต่ Phase 7** อย่ารอจนเสร็จหมด
5. **อัปเดตไฟล์ `plan.md` นี้ทุกครั้งที่มีการเปลี่ยนแปลง**

---

## สิ่งที่ยังต้องตัดสินใจ

- [ ] **ไฟล์แนบ**: อนุญาตชนิดไหน (PDF/Word/Excel/รูป), ขนาดสูงสุดเท่าไหร่, กี่ไฟล์ต่อรายงาน
- [ ] **ผู้รับผิดชอบ**: 1 ส่วนงานมีผู้ใช้กี่คน, ใครเป็นคนแจ้งรายชื่อและอีเมล
- [ ] **ช่วงเวลาเปิด-ปิดจริง**: ตอนนี้ตั้งค่าไว้แบบคาดการณ์ (เปิดหลังจบไตรมาส ให้เวลา 1 เดือน) ต้องแก้ให้ตรงปฏิทินจริง
- [ ] **คะแนน 1–5**: ให้ระบบคำนวณอัตโนมัติอย่างเดียว หรือให้ผู้ประเมินปรับด้วยมือได้
- [x] **ตัวชี้วัดปีถัดไป**: มีปุ่มคัดลอกจากปีก่อนให้แล้วที่ `/admin/fiscal-years` (คัดลอกมาแล้วค่อยแก้เฉพาะที่เปลี่ยน)
      ยังไม่ต้องกดตอนนี้ รอถึงเวลาขึ้นปี 2570 จริง
- [ ] **Domain**: จะใช้ชื่อเว็บของ Vercel หรือใช้โดเมนขององค์กร

---

## บันทึกการเปลี่ยนแปลง

| วันที่ | การเปลี่ยนแปลง |
|---|---|
| 6 ก.ย. 2569 | วางแผนระบบครั้งแรก · เลือกเทคโนโลยี · ออกแบบฐานข้อมูล 9 ตาราง |
| 6 ก.ย. 2569 | ยืนยัน 3 ข้อ: ส่วนกลางสร้างตัวชี้วัด, ไม่มีขั้นอนุมัติ, login บัญชีในระบบ |
| 6 ก.ย. 2569 | เสร็จ Phase 1–2 · พบไฟล์ MOU จริง · ได้รายชื่อ 30 หน่วยงานจริง |
| 6 ก.ย. 2569 | ปรับ schema ตาม MOU จริง: เพิ่ม `dimension`, `direction`, `ScoreCriteria.targetValue` |
| 7 ก.ย. 2569 | ยืนยันการจัดประเภทหน่วยงาน (ไม่สังกัดส่วนงาน 10 หน่วย) |
| 7 ก.ย. 2569 | ดึงตัวชี้วัด 301 รายการจาก MOU (Word) ครบ 30 ฉบับ · เพิ่ม `baselineValue`, `groupName`, `adjustmentNote` |
| 7 ก.ย. 2569 | เสร็จ Phase 3 ระบบ Login + สิทธิ์ตาม role |
| 7 ก.ย. 2569 | **เปลี่ยนเป้าหมายการ deploy เป็น Vercel** → ไฟล์แนบต้องย้ายไป Vercel Blob |
| 7 ก.ย. 2569 | สร้าง git repo และ push ขึ้น GitHub (`panpilin443651-wq/mou`) · กันเอกสารต้นฉบับ 62 ไฟล์ไม่ให้ขึ้น git |
| 7 ก.ย. 2569 | เชื่อมต่อ Neon สำเร็จ · รัน migration และใส่ข้อมูลตั้งต้นครบ · ทดสอบ login จริงผ่าน |
| 7 ก.ย. 2569 | เสร็จ Phase 4: จัดการตัวชี้วัด (รายการ+ค้นหา+กรอง+แบ่งหน้า, รายละเอียด, เพิ่ม, แก้ไข) และหน้าเปลี่ยนรหัสผ่าน · ทดสอบการเจาะสิทธิ์ 6 รูปแบบผ่านทั้งหมด |
| 7 ก.ย. 2569 | เสร็จ Phase 5: จัดการผู้ใช้ (เพิ่ม/แก้/ตั้งรหัสผ่านใหม่) และปีบัญชี (เพิ่มปี/สลับปี/คัดลอกตัวชี้วัด) · เพิ่ม `src/lib/datetime.ts` รวมการแปลงเวลาไทย ↔ UTC ไว้ที่เดียว |
| 7 ก.ย. 2569 | พบว่าเครือข่ายที่ใช้อยู่บล็อกพอร์ต 5432 ทำให้รันเว็บบนเครื่องแล้วต่อฐานข้อมูลไม่ได้ (ไม่ใช่ปัญหาโค้ด) · แก้โดยเปลี่ยนเน็ต |
| 7 ก.ย. 2569 | ทดสอบ Phase 5 กับฐานข้อมูลจริงผ่านหน้าเว็บจริงครบทุกเส้นทาง · เพิ่มปีบัญชี 2570 และคัดลอกตัวชี้วัด 301 รายการเรียบร้อย |
| 7 ก.ย. 2569 | เสร็จ Phase 10: ตรวจทุกหน้าบนจอมือถือแบบอัตโนมัติ 16 หน้า · แก้ปุ่มที่เล็กเกินนิ้วทั้งระบบ จาก 16 หน้าที่มีปัญหาเหลือ 0 |
| 7 ก.ย. 2569 | **แก้ปัญหาเน็ตบล็อกพอร์ต 5432 แบบถาวร** ด้วย `@prisma/adapter-neon` ต่อฐานข้อมูลผ่าน HTTPS ใช้ได้ทุกเครือข่าย |
| 7 ก.ย. 2569 | เสร็จ Phase 9: หน้าภาพรวม (ตัวเลขสรุป · สรุปตามมิติ · เปรียบเทียบ 30 ส่วนงาน · กรองรายไตรมาส) และดาวน์โหลดสรุปเป็นไฟล์ Excel 3 แผ่นงาน |
| 7 ก.ย. 2569 | เสร็จ Phase 8: ตั้งเวลาเปิด-ปิดระบบรายไตรมาส · ปิดฉุกเฉิน · ขยายเวลาเฉพาะส่วนงาน · ตรวจที่เซิร์ฟเวอร์ทุกจุดที่เขียนข้อมูล (กรอกผล/ดึงกลับมาแก้/ออกบัตรผ่านอัปโหลด/บันทึกไฟล์/ลบไฟล์) |
| 7 ก.ย. 2569 | เขียนส่วนไฟล์แนบเสร็จ (Vercel Blob · แยกตามระดับคะแนน 1-5 · PDF/Word/Excel/รูป ไม่เกิน 10 MB ต่อไฟล์) รอ token เพื่อทดสอบ |
| 7 ก.ย. 2569 | เสร็จ Phase 7 (ส่วนรายงานผล): กรอกผลรายไตรมาส คำนวณ % และคะแนน 1-5 อัตโนมัติ ปรับคะแนนด้วยมือได้ · **แก้สูตร % ให้รองรับตัวชี้วัดที่ค่าน้อยยิ่งดี** · เจอและแก้บั๊กค่าในช่องตัวเลือกหายหลังบันทึกไม่ผ่าน (กระทบ 3 ฟอร์ม) |
| 7 ก.ย. 2569 | เสร็จ Phase 6: แผนการดำเนินงาน (รายการแผน 301 ตัวชี้วัด, วางกิจกรรมรายไตรมาส, แก้ในที่เดิม, แถบความคืบหน้า) · ทดสอบด้วยเบราว์เซอร์จริงและทดสอบเจาะสิทธิ์ 5 รูปแบบผ่านทั้งหมด |
